WHAT I DO FOR ...

* For up and down data give frame with [emetter, dest, action(up, down), payload, timestamp, crc] :
    - using Observer.
    - using Chain of Responsability.

* For payload in frame test object.__dict__ to json()

* For call a module in another :
    *   For exemple : In module "Configuration" call : "from mdlbase.managers import MdlBaseManager"

* For verify commands in the terminal.
    *   Take a specific json file and verify the command with it. 